# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy Firebase Cloud Function
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Installations
        run: |
          cd functions
          npm ci
          npm run build
      - name: Deployment
        uses: w9jds/firebase-action@main
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ROOMEIGHT_9CD94 }}'
          channelId: live
          projectId: roomeight-9cd94
          args: deploy --only functions
